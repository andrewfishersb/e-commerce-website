<div class="row">
  <div class="col-md-9">
    {{#each model as |product|}}
      {{product-tile product=product}}
    {{/each}}
  </div>
  <div class="col-md-3">
    <div class="row">
      {{display-cart}}
    </div>
  </div>
</div>
