
<div class="row">
  <div class="col-md-6">
    <img src={{product.image}}>
  </div>
  <div class="col-md-6">
    <h3>{{product.name}}</h3>
    <p>{{product.description}}</p>
    <p><strong>{{product.price}}</strong></p>
  </div>
</div>
